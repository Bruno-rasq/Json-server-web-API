{
  "name": "Json Server Web API",
  "version": "1.0.0",
  "description": "This is a Json Server Web API based on file data.json.",  
  "scripts": {    
    "db": "json-server --watch data.json --host 0.0.0.0 --port 80"
  },
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.0.6",
    "json-server": "^0.17.3",
    "node-fetch": "^3.2.6"
  }
}
